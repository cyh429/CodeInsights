# 2026-05-22 阶段 14B Pipeline Runner v2 默认化

## 结论

阶段 14B 已完成 Pipeline Runner v2 默认化：未设置 `RV_AGENT_RUNTIME_PIPELINE_RUNNER_V2` 时默认走 `InProcessAgentRuntimeRunner`，显式设置 `RV_AGENT_RUNTIME_PIPELINE_RUNNER_V2=0` 可回到 Pipeline legacy adapter，显式开启值继续强制 Runner v2。

本阶段没有默认开启 `agentRuntimeChannelsV2`，也没有删除旧 Agent 主循环、Pipeline legacy adapter、旧 Feishu bridge 或旧 session JSONL 兼容。

阶段提交：`be82e53d feat(agent): 完成阶段14B Pipeline Runner v2 默认化`。

版本：

- `@rv-insights/shared`: `0.1.40`
- `@rv-insights/electron`: `0.0.97`

## 实现范围

- `agentRuntimePipelineRunnerV2` 默认策略从“仅 env 显式 `1` 开启”调整为“默认开启，显式关闭回滚”。
- 显式关闭值：`0`、`false`、`off`、`no`、`disabled`。
- 显式开启值：`1`、`true`、`on`、`yes`、`enabled`。
- 空值或未知值按新默认处理为开启。
- 新增模块导入隔离测试，验证 env 在导入时就能正确决定模块级 flag。
- 新增 Pipeline 节点分支日志，用于真实 Electron 验证 runner 分支。
- `agentRuntimeChannelsV2` 仍保持 env 显式 `1` 才启用。

## 默认化前验证

自动验证全部通过：

```bash
bun run typecheck
```

```bash
bun test apps/electron/src/main/lib/agent-runtime-runner.test.ts apps/electron/src/main/lib/agent-orchestrator/completion-signal.test.ts apps/electron/src/main/lib/agent-runtime-event-log.test.ts apps/electron/src/renderer/atoms/agent-atoms.test.ts packages/shared/src/agent/runtime-events.test.ts apps/electron/src/main/lib/feishu-channel-adapter.test.ts
```

结果：51 pass。

```bash
bun test apps/electron/src/main/lib/pipeline-node-runner.test.ts apps/electron/src/main/lib/pipeline-human-gate-service.test.ts apps/electron/src/main/lib/pipeline-patch-work-service.test.ts apps/electron/src/main/lib/codex-pipeline-node-runner.test.ts
```

结果：88 pass。

```bash
env -i HOME=/tmp/rv-clean-home-stage14b-pre USERPROFILE=/tmp/rv-clean-home-stage14b-pre TMPDIR=/tmp PATH=/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin SHELL=/bin/zsh /opt/homebrew/bin/bun test apps/electron/src/main/lib/codex-pipeline-node-runner.test.ts
```

结果：30 pass。

```bash
git diff --check
```

结果：通过。

真实 Electron 默认化前 Pipeline v2 显式开启验证：

- Electron 版本：`@rv-insights/electron/0.0.96`
- env：`RV_AGENT_RUNTIME_PIPELINE_RUNNER_V2=1`、`RV_AGENT_RUNTIME_RUNNER_V2=1`
- 成功 sessionId：`de721097-fdb8-4cc3-9847-e5707eecb771`
- 失败但有用的 unseeded sessionId：`35bfc6af-233b-4318-8564-a66691d44bbb`，因 session cwd 无 Git repository，Git guard fail closed。
- 成功 run 到达 `explorer -> planner -> developer -> reviewer -> tester -> committer`，最终 `completed`，`lastApprovedNode=committer`。
- Git guard 复验：HEAD `009d6c3625fb3991be34037e5c26eb520c3d30e5` 未变，refs 仅 `refs/heads/main`，staged diff 为空，index/config SHA256 未变。
- patch-set 排除 `patch-work/**`，`test-evidence.json` 全部为 `passed`，commit / PR 仅生成 draft。

## 默认化后验证

自动验证全部通过：

```bash
bun run typecheck
```

```bash
bun test apps/electron/src/main/lib/agent-runtime-runner.test.ts apps/electron/src/main/lib/agent-orchestrator/completion-signal.test.ts apps/electron/src/main/lib/agent-runtime-event-log.test.ts apps/electron/src/renderer/atoms/agent-atoms.test.ts packages/shared/src/agent/runtime-events.test.ts apps/electron/src/main/lib/feishu-channel-adapter.test.ts
```

结果：51 pass。

```bash
bun test apps/electron/src/main/lib/pipeline-node-runner.test.ts apps/electron/src/main/lib/pipeline-human-gate-service.test.ts apps/electron/src/main/lib/pipeline-patch-work-service.test.ts apps/electron/src/main/lib/codex-pipeline-node-runner.test.ts
```

结果：91 pass。

说明：一次全量 Pipeline 聚焦测试中，`Codex CLI runner 在 v2 tester 中阻断 Git/PR 命令并回滚绝对路径 Git 污染` 因默认 5s 超时出现假阴性；单独重跑通过，随后将该长 Git guard 用例超时调整为 10s，再全量复跑 91 pass。

```bash
env -i HOME=/tmp/rv-clean-home-stage14b-final USERPROFILE=/tmp/rv-clean-home-stage14b-final TMPDIR=/tmp PATH=/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin SHELL=/bin/zsh /opt/homebrew/bin/bun test apps/electron/src/main/lib/codex-pipeline-node-runner.test.ts
```

结果：30 pass。

真实 Electron 默认开启验证：

- Electron 版本：`@rv-insights/electron/0.0.97`
- env：未设置 `RV_AGENT_RUNTIME_PIPELINE_RUNNER_V2`
- sessionId：`a70c02d0-ff2f-4283-b121-cd963771fd9f`
- channelId：`0c21ad0d-f1c8-4e67-809d-5b8e9bf64430`
- workspaceId：`f06e78c4-dd1b-4ff5-b3a1-123919e99bcd`
- 种子仓库：`~/.rv-insights-dev/agent-workspaces/default/a70c02d0-ff2f-4283-b121-cd963771fd9f`
- 结果：`explorer -> planner -> developer -> reviewer -> tester -> committer`，最终 `completed`，`lastApprovedNode=committer`。
- 路径证据：Electron 日志出现 `[Pipeline Node Runner] explorer 使用 InProcessAgentRuntimeRunner 执行` 和 `[Pipeline Node Runner] planner 使用 InProcessAgentRuntimeRunner 执行`。
- Git guard 复验：HEAD `f908d9fc45795b5a3e65fcaec649db2e18b0a0ed` 未变，refs 仅 `refs/heads/main`，staged diff 为空，index SHA256 `5c3fac2a3d81eb4ad1e58c8b84c77882ba4c7e7b1cfa6a1d59d2062b89261d48` 未变，config SHA256 `6260efbdf5ce8288e0724fe95d167459922c83ad7e32070da3b7e1362d1518f8` 未变。
- patch-work 文件完整：`explorer/report-001.md`、`selected-task.md`、`plan.md`、`test-plan.md`、`dev.md`、`review.md`、`result.md`、`commit.md`、`pr.md`、`patch-set/*`。
- patch-set changed files：`src/sum.js`、`tasks/todo.md`、`test/sum.test.js`。
- `test-evidence.json`：`bun test` 与 `bun test --coverage` 均为 `passed`。
- commit / PR 草稿明确 draft-only，未执行 `git add`、`git commit`、`git push` 或创建 PR。

真实 Electron 显式关闭回滚验证：

- Electron 版本：`@rv-insights/electron/0.0.97`
- env：`RV_AGENT_RUNTIME_PIPELINE_RUNNER_V2=0`
- sessionId：`1112d7fc-ab4b-4e4b-bedf-193533a7daec`
- channelId：`0c21ad0d-f1c8-4e67-809d-5b8e9bf64430`
- workspaceId：`f06e78c4-dd1b-4ff5-b3a1-123919e99bcd`
- 结果：启动 explorer 后手动 stop，最终状态 `terminated`。
- 路径证据：Electron 日志出现 `[Pipeline Node Runner] explorer 使用 legacy adapter 执行`。
- 说明：本回滚验证只证明显式关闭 env 会回到 legacy adapter 分支；默认路径深水位已由 session `a70c02d0-ff2f-4283-b121-cd963771fd9f` 覆盖。

## 飞书状态

未补跑。

阻塞原因：

- 本机缺少 `~/.rv-insights/feishu.json`。
- 本机缺少 `~/.rv-insights-dev/feishu.json`。

要求：

- 不得伪造飞书入口或飞书群聊 MCP 通过。
- `agentRuntimeChannelsV2` 继续默认关闭，直到补齐真实飞书配置和入口验证。

## 回滚点

- 设置 `RV_AGENT_RUNTIME_PIPELINE_RUNNER_V2=0` 可回到 Pipeline legacy adapter。
- 旧 Agent 主循环、Pipeline legacy adapter、旧 Feishu bridge 和旧 session JSONL 兼容均保留。
- `agentRuntimeChannelsV2` 仍默认关闭。

## 下一步

- 阶段 14B 已单独提交：`be82e53d feat(agent): 完成阶段14B Pipeline Runner v2 默认化`。
- 阶段 14C Channels v2 默认化继续受飞书配置缺失阻塞；只有补齐真实飞书配置后才能评估。
- 删除旧 Agent 主循环、Pipeline legacy adapter、旧 Feishu bridge 或旧 session JSONL 兼容只能放到后续稳定阶段。
