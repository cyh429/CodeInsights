# 2026-05-17 Agent 阶段 0 基线首轮

## 运行范围

- 日期：2026-05-17
- 项目路径：`/Users/zq/Desktop/ai-projs/posp/CodeInsights`
- 配置目录：`~/.codeinsights-dev/`
- 当前阶段：阶段 0，冻结重构前行为证据。
- 业务代码修改：无。
- 客户端 UI 可见变化：无。本轮只新增文档证据和更新开发清单。

## 本地环境快照

- Agent channel：`0c21ad0d-f1c8-4e67-809d-5b8e9bf64430`
- Provider：`deepseek`
- 当前模型：`deepseek-v4-flash`
- 当前 workspaceId：`f06e78c4-dd1b-4ff5-b3a1-123919e99bcd`
- 当前 workspace slug：`default`
- 当前 activeTabId：`0ec2d089-e052-4d20-9ed6-57746cf4ac29`
- Agent session 索引：`~/.codeinsights-dev/agent-sessions.json`
- Agent JSONL 目录：`~/.codeinsights-dev/agent-sessions/`
- Agent workspace 目录：`~/.codeinsights-dev/agent-workspaces/default/`
- 当前未发现飞书配置文件。
- 当前 workspace 根下未发现 `mcp.json`；存在 `.claude-plugin/plugin.json`。
- 当前 workspace skills 中发现：`brainstorming`、`docx`、`executing-plans`、`find-skills`、`pdf`、`pptx`、`skill-creator`、`tool-builder`、`writing-plans`、`xlsx`。

## 存量 session 证据

| sessionId | title | channelId | sdkSessionId | JSONL 行数 | 终态 |
| --- | --- | --- | --- | ---: | --- |
| `0ec2d089-e052-4d20-9ed6-57746cf4ac29` | 曼联赛程与近期战况 | 旧记录为空 | `d4de1bc5-93d6-46bd-bb6f-6450ad95e8e2` | 40 | `result.subtype=success`，`terminal_reason=completed` |
| `11c4068d-a113-4275-adb4-996dea0c0924` | 你好 | 旧记录为空 | `641d0160-f22b-470b-a19c-5d1b67a88634` | 10 | 前 3 次渠道/模型错误，最终 `completed` |
| `2bc3930d-ee2b-4792-a985-6b3366e25e22` | 你好 | `0c21ad0d-f1c8-4e67-809d-5b8e9bf64430` | `fcf5debd-33f0-4267-99dd-105e82f03860` | 4 | `result.subtype=success`，`terminal_reason=completed` |
| `c9402de0-f460-4c7d-b0d1-d889f8bd5b1a` | 新 Agent 会话 | `0c21ad0d-f1c8-4e67-809d-5b8e9bf64430` | 无 | 无 JSONL | 新建未运行 |

SDKMessage JSONL 当前格式：

- 用户输入行：`type=user`，`message.content` 为 text block 数组，包含 `_createdAt`。
- assistant 流式行：`type=assistant`，`message.role=assistant`，`message.model=deepseek-v4-flash`，`content` 可包含 `thinking`、`tool_use`、`text`。
- tool result 行：仍写成 `type=user`，`message.role=user`，content 中包含 `tool_result`。
- 终态行：`type=result`，`subtype=success`，包含 `duration_ms`、`num_turns`、`session_id`、`usage`、`modelUsage`、`permission_denials`、`terminal_reason=completed`。
- 错误样例：`type=assistant`，content 为错误文案，包含 `error.errorType`、`_errorCode`、`_errorActions`。

## 基线场景

### 1. 首条消息

- 本轮证据：存量 session `2bc3930d-ee2b-4792-a985-6b3366e25e22`。
- 输入：新 session 发送 `你好`，workspaceId `f06e78c4-dd1b-4ff5-b3a1-123919e99bcd`，channelId `0c21ad0d-f1c8-4e67-809d-5b8e9bf64430`，模型 `deepseek-v4-flash`。
- 预期 UI：消息列表出现用户消息、assistant thinking/text 输出；输入区从 running 恢复可编辑；标题从默认标题更新为短标题。
- 预期存储：`agent-sessions.json` 写入 session metadata；`agent-sessions/{sessionId}.jsonl` 追加 `user -> assistant -> assistant text -> result`。
- 预期终态：`result.subtype=success`，`terminal_reason=completed`，`sdkSessionId` 回写到 metadata。
- 实跑状态：已由存量 JSONL 证明。

### 2. 同会话并发

- 本轮证据：代码与存量行为约束，未做实时 UI 双发送。
- 输入：第一条 run 未结束时，在同 session 再发送第二条。
- 预期 UI：第二条应被阻止或进入当前版本的队列策略；同一 session 不出现两个并行 running 指示。
- 预期存储：不产生重复 SDK run；若进入队列，只追加去重后的 queued user message。
- 预期终态：第一条 run 正常进入 completed/stopped/failed，第二条按当前队列策略后续执行或被提示。
- 当前实现依据：`AgentOrchestrator` 有 `activeSessions` 并发守卫和 `queuedMessageUuids` 防重记录。
- 实跑状态：待后续有交互式桌面环境时补跑。

### 3. 用户停止

- 本轮证据：代码路径已确认，当前本地 JSONL 无 stopped session 样例。
- 输入：发送长任务后点击停止。
- 预期 UI：loading 清理，停止按钮恢复，输入区恢复；消息流展示已停止或保留部分输出。
- 预期存储：不再追加新的 assistant/tool 事件；metadata `stoppedByUser=true`；可能保留中止前已收到的 SDKMessage。
- 预期终态：`run_stopped(reason=user_abort)`；旧路径中应通过 completion signal 标记 `stoppedByUser`。
- 实跑状态：待补跑。

### 4. 权限批准

- 本轮证据：代码路径已确认，当前 JSONL 无权限请求样例。
- 输入：触发 `Write` / `Edit` / 非只读 `Bash`，在权限横幅选择 approve。
- 预期 UI：权限横幅出现后消失；对应工具继续执行；消息流继续追加工具结果和 assistant 输出。
- 预期存储：旧 JSONL 主要保存 SDKMessage；权限 requested/resolved 当前通过主进程 pending map 和 renderer queue 可追踪，阶段 2 后需落入 events JSONL。
- 预期终态：run 继续，最终 completed 或进入下一个 pending interaction。
- 当前实现依据：`AgentPermissionService.respondToPermission()` 对 allow 返回 `{ behavior: "allow" }`，`alwaysAllow` 会加入 session 白名单。
- 实跑状态：待补跑。

### 5. 权限拒绝

- 本轮证据：代码路径已确认，当前 JSONL 无 `permission_denials` 非空样例。
- 输入：触发写文件或 shell 操作，在权限横幅选择 deny。
- 预期 UI：权限横幅消失；工具结果展示被拒绝；run 不能卡住。
- 预期存储：旧 result 行应可记录 `permission_denials`；阶段 2 后需记录 `permission_resolved(decision=denied)`。
- 预期终态：SDK 继续自我修正或 run stopped/failed，但 pending 请求必须清理。
- 当前实现依据：`respondToPermission()` 对 deny 返回 `{ behavior: "deny", message: "用户拒绝了此操作" }`。
- 实跑状态：待补跑。

### 6. AskUser

- 本轮证据：代码路径已确认，当前 JSONL 未发现 `AskUserQuestion`。
- 输入：触发 `AskUserQuestion`，切到设置页，再回到 Agent 会话响应。
- 预期 UI：AskUser 横幅按 sessionId 保留；页面切换后 pending 问题不丢；响应后横幅消失并继续 run。
- 预期存储：当前旧路径主要由 `agent-ask-user-service` 的 pending map 和 renderer atom 保留；阶段 2 后需记录 ask requested/resolved。
- 预期终态：run 继续 completed，或在用户取消时进入 stopped/failed；不能永久 pending。
- 实跑状态：待补跑。

### 7. Plan Mode

- 本轮证据：代码路径已确认，当前 JSONL 未发现 plan mode 样例。
- 输入：触发进入计划模式，再批准或退出计划模式。
- 预期 UI：计划交互与普通权限横幅可区分；用户能明确 approve/deny/continue。
- 预期存储：当前旧路径通过 exit plan service / permission dispatcher 管理；阶段 2 后需记录 plan mode requested/resolved。
- 预期终态：run 继续或等待用户，pending 不丢失。
- 实跑状态：待补跑。

### 8. MCP 可见性

- 本轮证据：本机 `~/.codeinsights-dev/agent-workspaces/default/` 下未发现 `mcp.json`。
- 输入：启用一个 workspace MCP server 后发送相关任务。
- 预期 UI：工具活动中可看到该 MCP 工具调用或可见能力。
- 预期存储：旧路径读取 workspace MCP 配置并传入 SDK；阶段 4/5 后 runtime manifest hash 应随 MCP 配置变化。
- 预期终态：run completed，或因权限进入 pending。
- 实跑状态：当前环境无 MCP server，待配置后补跑。

### 9. Skill mention

- 本轮证据：workspace 下已有多个 Skill 目录。
- 输入：在 prompt 中提及已启用 Skill，例如 `xlsx` / `pdf` / `docx`。
- 预期 UI：如果 SDK 使用 Skill，消息流仍按普通 assistant/tool 活动展示，不新增可见 UI。
- 预期存储：旧路径依赖 workspace skills 目录和动态上下文；阶段 4/5 后 skill snapshot/hash 可追踪。
- 预期终态：run completed。
- 实跑状态：已确认本地 skill 文件存在；未做模型实跑。

### 10. 文件上传

- 本轮证据：当前未发现本轮附件样例。
- 输入：上传附件后发送总结任务。
- 预期 UI：附件 chip/引用出现在输入或消息上下文；assistant 能基于附件内容回答。
- 预期存储：附件写入 `~/.codeinsights-dev/attachments/{conversationId}/` 或 Agent 对应 attachment ref；session JSONL 记录用户消息与附件引用。
- 预期终态：run completed。
- 实跑状态：待补跑。

### 11. 附加目录

- 本轮证据：当前未发现 attached directories 配置样例。
- 输入：配置 additional directory 后要求读取其中允许文件。
- 预期 UI：需要权限时出现权限横幅；允许后展示读取结果。
- 预期存储：旧 workspace 配置保留 directory ref；阶段 4/5 后 manifest 记录 additionalDirectories。
- 预期终态：run completed 或 pending permission，不应越权访问未授权目录。
- 实跑状态：待补跑。

### 12. Resume

- 本轮证据：存量 session `0ec2d089-e052-4d20-9ed6-57746cf4ac29` 在同一 JSONL 中有多轮用户输入，且 metadata 保留 `sdkSessionId=d4de1bc5-93d6-46bd-bb6f-6450ad95e8e2`。
- 输入：打开旧 session 继续发送，例如在同一会话先问曼联赛程，再继续问南京天气。
- 预期 UI：历史消息可见，新回复沿用同一会话上下文；标题与 tab 不被重置。
- 预期存储：同一个 `{sessionId}.jsonl` 继续追加；result 行保留同一或兼容的 SDK `session_id`。
- 预期终态：run completed。
- 实跑状态：已由存量 JSONL 多轮记录证明。

### 13. Fork

- 本轮证据：代码能力存在，当前未发现 fork 产物样例。
- 输入：从旧消息 fork。
- 预期 UI：建立新 session，源 session 历史不变；新 tab/session 显示 fork 后上下文。
- 预期存储：新 session metadata 记录来源或通过复制后的 JSONL 保留 fork 起点；源 JSONL 不被修改。
- 预期终态：新 run completed。
- 实跑状态：待补跑。

### 14. Rewind

- 本轮证据：代码能力存在，当前未发现 rewind 样例。
- 输入：回退到历史消息点后继续发送。
- 预期 UI：回退点之后的上下文被隐藏或截断，新回复基于回退点继续。
- 预期存储：旧路径应通过 `truncateSDKMessages` / `rewindFilesFromSnapshot` 等能力处理；阶段后续需记录 `resumeAtMessageUuid` 或 fallback。
- 预期终态：run completed。
- 实跑状态：待补跑。

### 15. 飞书入口

- 本轮证据：当前配置目录未发现飞书配置文件。
- 输入：从飞书 chat 触发 Agent。
- 预期 UI：桌面端可见对应 session 或后台状态；飞书收到降级输出。
- 预期存储：channel target/binding 记录到飞书相关配置或 session metadata。
- 预期终态：run completed，外部渠道不默认绕过权限。
- 实跑状态：当前环境无飞书配置，待配置后补跑。

### 16. 飞书群聊 MCP

- 本轮证据：当前配置目录未发现飞书配置文件，也无 channel-scoped MCP overlay 样例。
- 输入：群聊动态 MCP 注入后触发 Agent。
- 预期 UI：桌面端 pending/结果能关联到该 run；群聊 MCP 只对该 run/channel 生效。
- 预期存储：run-scoped MCP overlay 记录，不写入 workspace manifest。
- 预期终态：run completed。
- 实跑状态：当前环境无飞书配置，待配置后补跑。

### 17. Pipeline 旁路

- 本轮证据：settings 中存在 pipeline tab；workspace 下存在 pipeline `patch-work/manifest.json`。
- 输入：Agent 重构前后执行现有 Pipeline 节点。
- 预期 UI：Pipeline tab、stage rail、records 和 gate 行为不受 Agent 重构影响。
- 预期存储：pipeline checkpoint / patch-work manifest 正常更新；Agent JSONL 不参与 Pipeline 节点状态。
- 预期终态：node completed。
- 实跑状态：存量文件证明 Pipeline 与 Agent storage 分离；未做新 Pipeline run。

## 首轮缺口

- 当前没有实时 Electron 桌面壳交互证据，因此并发、停止、权限 approve/deny、AskUser、Plan Mode、附件、additional directory、fork、rewind、飞书入口需要后续补跑。
- 当前没有 workspace MCP server 配置，因此 MCP 可见性只能记录预期和缺口。
- 当前没有飞书配置，因此飞书两条基线不应伪造为通过。
- 后续阶段如果触碰 Agent runtime、event log、renderer reducer、workspace materializer 或外部渠道，必须优先补跑相关缺口。

## 验证命令

阶段 0 文档完成后运行：

```bash
bun run typecheck
git diff --check -- docs/agent-refactor tasks/todo.md
```

