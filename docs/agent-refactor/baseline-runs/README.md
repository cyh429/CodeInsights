# Agent 重构基线证据目录

本目录保存 Agent runtime 重构前的文本基线证据。

约束：

- 只保存小型文本证据，不保存截图、录屏或大体积日志。
- 不记录 API Key、加密密文、用户隐私文件内容或完整大段模型输出。
- 每轮基线使用独立 Markdown 文件，文件名格式建议为 `YYYY-MM-DD-round-N.md`。
- 每条基线都要记录输入、预期 UI、预期存储和预期终态。

